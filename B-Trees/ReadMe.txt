Input
When prompted, enter an integer value N. This value determines the number of unique random integers to be generated and inserted into the B-tree.
Example: If you enter 500, the program will generate 500 unique random integers. which will then be used to create a B-tree